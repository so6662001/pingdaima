# 开发计划（AI 逐任务执行）

> 本文是 AI 编码助手的施工图。执行规则见 [`AGENTS.md`](../../AGENTS.md)，进度记录在 [`TASKS.md`](TASKS.md)。
>
> 每个任务的「输入」列指明必须先读的 PRD 章节与原型页面；「验收」列是该任务可执行的检查项。任务之间按编号顺序执行，同批次内可并行的已在依赖中标注。

## 批次总览

| 批次 | 主题 | 任务数 | 对应 PRD 发布批次 |
| --- | --- | --: | --- |
| M0 | 工程基座、领域契约与设计系统迁移 | 5 | 前置 |
| M1 | 数据库、状态机引擎、认证鉴权 | 5 | 前置 |
| M2 | 工单与客户 | 6 | R1 |
| M3 | 需求池与优先级 | 9 | R1 |
| M4 | 项目与迭代 | 6 | R1 |
| M5 | 研发看板 | 7 | R1 |
| M6 | 代码与持续交付 | 4 | R1 |
| M7 | 测试与质量 | 6 | R2 |
| M8 | 产品与发布 | 4 | R2 |
| M9 | 智能工时与成本 | 5 | R2 |
| M10 | 度量与人效 | 4 | R3 |
| M11 | 复盘与知识库 | 4 | R3 |
| M12 | 消息与业务方门户 | 5 | R2 / R3 |
| M13 | 系统设置与整体收尾 | 5 | 收尾 |

---

## M0 工程基座、领域契约与设计系统迁移

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0001 | 建仓库骨架与构建 | `docs/dev/TECH-STACK.md` | `backend/` Maven 多模块（common / domain / infra / app）、`frontend/` Vite + Vue 3 + TS、`docker-compose.yml`（MySQL 8 + Redis 7 + RabbitMQ）、根 `Makefile`（全部命令）、Spotless + Checkstyle + ESLint + Prettier、`.env.example`、GitHub Actions | `make up && make build && make lint && make test` 全绿；`make dev` 前后端可访问 |
| T-0002 | 领域契约与代码生成 | `docs/prd/state-machines.md`、`docs/PRD.md` 5.2~5.6、`docs/prd/metrics.md`、`docs/prd/data-model.md#四枚举字典`；规则 `13-contracts` | `contracts/domain/{enums.yaml,algorithms.yaml,metrics.yaml,state-machines/*.yaml}`；`tools/codegen` 生成 Java 常量/枚举/转换表与 TS 对应物；生成物带 DO NOT EDIT 头 | `make codegen` 后 `git diff --exit-code` 干净；生成器自身有单测；`make check-prd` 通过；YAML 中的参数值与 PRD 逐一核对无差异 |
| T-0003 | 设计系统迁移（Vue） | `assets/css/app.css`、`assets/js/{icons,charts,ui,layout}.js`；规则 `03-ui-fidelity` | `frontend/src/ui/`：`styles/app.css`（原样复制）、`Icon.vue`、`charts/*`（line/area/bar/stackedBar/stackedArea/donut/gauge/radar/heatmap/sparkline/hbar）+ `Chart.vue`、`Toast/Drawer/Modal/Tabs/Switch/Checkbox/Menu/Kanban`、`layout/{AppLayout,Sidebar,Topbar,CommandPalette,NotifyCenter}` | 建 `/kitchen-sink` 演示页渲染全部组件与图表，与原型同类元素逐像素比对差异 ≤ 1% |
| T-0004 | 抽取原型数据为 fixtures | 24 个原型页面内联 `<script>` 中的数据数组 | `contracts/fixtures/*.json`：users、teams、customers、tickets、products、requirements、projects、sprints、workItems、testCases、releases、worklogs、retros、notifyRules 等，字段名对齐 `docs/prd/data-model.md` | 记录数与原型一致；有一致性测试断言关键条目（REQ-2041、STORY-3313、TK-3821、PRJ-018）字段值与原型相同 |
| T-0005 | 校验脚本与 CI 接线 | `tools/check-prd-refs.mjs`、`tools/check.sh` | `make check-prd`、`make parity`、`e2e/playwright.parity.config.ts`（静态服务指向仓库根目录）、CI 跑 lint/test/codegen-diff/check-prd | `make verify` 可一次跑通；`make parity` 能产出 diff 图 |

**批次验收**：骨架可运行；契约生成物与 PRD 参数一致且无 diff；设计系统演示页还原达标；fixtures 一致性测试通过；CI 全绿。

---

## M1 数据库、状态机引擎、认证鉴权

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0101 | MySQL 表结构与 Flyway 迁移 | `docs/prd/data-model.md` 全文 | `backend/db/migration/V1__init.sql` 等：全部实体、字段、索引、唯一约束；utf8mb4、`DATETIME(3)` 存 UTC、`DECIMAL(18,4)` 金额、原生 JSON 字段、`is_deleted` 软删除、无物理外键 | 有测试遍历库表断言关键表与唯一约束存在；`make migrate` 在干净库上成功；重复执行幂等 |
| T-0102 | 领域模型与 MyBatis 层 | T-0101 表结构、T-0002 契约枚举 | 领域对象、Mapper 与 XML、TypeHandler（JSON 字段）、分页封装、软删除、数据范围拦截器 | Testcontainers 起真实 MySQL 跑 Mapper 测试；分页与软删除行为正确 |
| T-0103 | 状态机引擎 | `docs/prd/state-machines.md`；T-0002 生成的转换表 | `StateMachineEngine.transition()`：合法性校验 → 守卫（返回全部未满足项）→ 事务内更新状态与 `state_transition_log`（含停留时长）→ 提交后发领域事件；强制流转需 reason 并写审计 | 8 类对象每条合法转换与非法转换均有单测；守卫失败返回具体未满足项；停留时长写入正确 |
| T-0104 | 种子数据 | `contracts/fixtures/*.json` | `make seed` 幂等灌库 | 重复执行不产生重复数据；各表记录数与 fixtures 一致 |
| T-0105 | 认证与鉴权 | `docs/PRD.md#22-角色与权限模型` | Spring Security + JWT 登录、企业微信 OAuth 预留、`@PreAuthorize` 方法级鉴权、查询层数据范围注入、越权 404、敏感字段脱敏 | 权限矩阵 10 项 × 5 角色测试全通过；越权断言 404；脱敏用例通过 |

**批次验收**：`make migrate && make seed` 后数据库内容与原型一致；状态机单测覆盖全部转换；权限矩阵测试全绿。

---

## M2 工单与客户

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0201 | 工单主流程与类型配置 | PRD 5.1 FR-TK-01/02；`docs/prd/state-machines.md#五工单-ticket`；`ticket.html` | 工单 CRUD、7 类工单类型与专属字段、渠道、指派 | US-TK-01/02 的 AC 全通过；自定义类型后表单即时生效（不发版） |
| T-0202 | SLA 时钟与升级 | PRD FR-TK-03；状态机文档的 SLA 规则 | 响应/解决双时钟、待客户确认暂停、30% 预警、归零升级、`escalation_level` | 时钟暂停与恢复的单测；预警与升级的定时任务测试；US-TK-03 AC 通过 |
| T-0203 | 工单池分诊 | PRD FR-TK-04；`docs/prd/flows.md#21-工单受理与分诊` | 相似度聚类（阈值 0.78）、六类判定、批量处理、合并时客户影响面叠加 | 相似度边界用例（0.77 / 0.78 / 0.79）；合并后加权呼声正确叠加 |
| T-0204 | 工单转需求与缺陷 | PRD FR-TK-05 | 一键转化、双向溯源、上线后回写工单 | 转化后 `req_source` 有记录；需求发布后来源工单出现"已交付" |
| T-0205 | 客户域 | PRD FR-CU-01/02/03 | 客户 360、等级系数（战略×3/KA×2/普通×1）、呼声矩阵加权排序、对客承诺与临期提醒 | 加权呼声计算单测；承诺临期定时提醒测试 |
| T-0206 | 页面还原 | `ticket.html`、`customer.html`；`docs/dev/UI-PARITY.md` | `/ticket`、`/customer` 两个页面全部页签与交互 | 两页视觉回归 ≤ 1%；分组切换（按状态/渠道/客户等级）、深链、分诊台交互均可用 |

**批次验收**：工单端到端串场（创建 → 分诊 → 转需求 → 关联客户）通过；两页还原达标。

---

## M3 需求池与优先级

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0301 | 需求主数据 | PRD FR-RQ-01；data-model 的 `requirement`/`req_source`/`req_customer` | 需求 CRUD、五类来源合流、来源单号保留、客户关联 | US-RQ-01 AC 通过；按客户呼声排序正确 |
| T-0302 | 富化、去重、归档 | PRD FR-RQ-02/03/10 | 富化建议（可逐段采纳）、相似度合并与撤销（7 天内）、归档库与复活 | 合并后呼声叠加、得分重算；撤销合并恢复原状；复活后按当前模型重新评分 |
| T-0303 | DoR 就绪度检查 | PRD FR-RQ-04（7 项检查） | 进入评审前的阻断校验、达标率统计、总监豁免（记录原因与审批人） | 未达标被拦截并返回未满足项清单；豁免路径有审计记录 |
| T-0304 | 优先级模型 | **PRD 5.2**；`requirement.html` 的模型页签 | 五种算法（加权÷工作量、RICE、WSJF、纯加权、自定义表达式）、六因素权重、合规强制置顶、模型版本化、得分快照与拆解 | 每种算法一组手工验算用例；权重变更生成新版本且历史快照不变；合规需求置顶 |
| T-0305 | 评审流程 | PRD FR-RQ-06；`docs/prd/flows.md#22-需求评审7-节点` | 7 节点、SLA 与超时推送、准入准出、价值评审会逐条投票与纪要 | 节点 SLA 超时推送测试；投票记录可导出 |
| T-0306 | 阶段计划 | **PRD 5.3** | 五阶段计划与实际、延期判定（≥1 天）、智能重排、按负责人/版本视图 | 延期边界用例（0.9 天不报、1 天报）；重排后后续阶段正确顺延 |
| T-0307 | 拆解与推送项目 | PRD FR-RQ-08/09 | 拆解模板、子项继承阶段计划与版本与客户、进度加权聚合、推送到项目（直接指派/投放任务池）、容量不足告警 | 子项继承字段正确；进度聚合与手工计算一致；容量不足时告警并可强制推送（记范围变更） |
| T-0308 | 需求池页面还原 | `requirement.html` | `/requirement` 五个页签（需求池、看板、清洗归档、优先级模型、评审、阶段排期）全部交互 | 视觉回归 ≤ 1%；四种看板分组、三种排期视图、算法切换均真实生效 |
| T-0309 | 需求详情页还原 | `requirement-detail.html` | `/requirement/:code` 全部页签与右侧属性面板、拆解工作台、价值与复盘 | 视觉回归 ≤ 1%；深链 `#REQ-2041` 可直达 |

**批次验收**：需求端到端（工单转入 → 富化 → DoR → 评分 → 评审 → 阶段计划 → 推送项目）通过；两页还原达标；优先级五种算法验算全对。

---

## M4 项目与迭代

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0401 | 项目与迭代主数据 | PRD FR-PJ-01；data-model | 项目组合、健康度、迭代（承诺基线冻结） | 迭代开始后承诺基线不可改，新增计入范围变更 |
| T-0402 | 多版本迭代规划 | PRD FR-PJ-02；`project-detail.html` | Backlog 与多 Sprint 并行规划、拖拽、容量实时校验、迭代目标 | 超容量列头告警；迭代中拖入提示范围变更并记录 |
| T-0403 | 资源排期与容量 | PRD FR-PJ-03；容量公式 | 成员×周热力矩阵、扣减项、跨项目冲突检测、快速排期 | 容量公式单测；>100% 告警；冲突清单正确 |
| T-0404 | 任务池与自助认领 | PRD FR-PJ-04 | 技能标签匹配、认领即分配与通知、4 小时超时推送（最多 3 次后升级） | 超时推送定时任务测试；认领后进入个人看板 |
| T-0405 | 项目页面还原 | `project.html`、`project-detail.html` | `/project`、`/project/:code` 全部页签 | 视觉回归 ≤ 1%；甘特、资源矩阵、迭代规划板交互可用 |
| T-0406 | 敏捷页面还原 | `agile.html` | `/agile`：站会看板、迭代节奏、规划扑克、健康雷达、依赖图 | 视觉回归 ≤ 1% |

**批次验收**：迭代规划 → 任务池 → 认领串场通过；容量与冲突计算正确；三页还原达标。

---

## M5 研发看板

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0501 | 看板配置 | PRD FR-BD-04；`board.html` 配置弹窗 | 列定义、WIP 上限、停留预警、准入准出策略、卡片显示字段、自动化流转开关 | 配置变更即时生效；列策略气泡展示与配置一致 |
| T-0502 | 五类看板数据服务与范围 | **PRD 5.6**、FR-BD-01/02 | 迭代/需求/缺陷/发布/我的五类看板；范围维度（全部/产品线/项目/团队）+ 迭代下拉；范围记忆与深链 | 每种范围的卡片集合正确；发布看板不提供按项目并给出说明；`#scope=prj:PRJ-021` 直达 |
| T-0503 | 流转与 WIP 判定 | PRD 5.6 | 拖拽走状态机、乐观更新与回滚、WIP 告警（跨项目按「项目+列」判定）、加急泳道不受限 | 跨项目视图不做 WIP 数值相加；超限提醒不硬阻断；非法流转被拒并提示原因 |
| T-0504 | 看板度量 | PRD FR-BD-07；`docs/prd/metrics.md` | CFD、周期时间散点（50%/85% 分位）、各列平均停留、WIP 趋势、阻塞 TOP、健康诊断 | 指标值与手工验算一致；诊断结论由规则生成而非硬编码文案 |
| T-0505 | 看板页面还原（重点） | `board.html`（全文，含内联样式与 900+ 行脚本） | `/board` 全部功能：五类看板、范围栏、泳道、密度、折叠列、卡片老化/阻塞/拉取信号、详情抽屉、度量抽屉、配置弹窗、全屏 | 视觉回归 ≤ 1%（四种范围 × 五类看板抽样）；`.cursor/rules/03-ui-fidelity.mdc` 的失误清单逐条自查通过 |
| T-0506 | 个人工作台还原 | `mywork.html` | `/mywork`：我的任务、子任务清单、推送状态、日程 | 视觉回归 ≤ 1%；`/board?bd=mine` 联动正确 |
| T-0507 | 首页工作台还原 | `index.html` | `/`：健康度、风险、客户之声、各模块入口 | 视觉回归 ≤ 1%；所有卡片数据来自真实接口 |

**批次验收**：看板四种范围 × 五类看板全部可用；拖拽流转经状态机校验；三页还原达标。

---

## M6 代码与持续交付

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0601 | GitHub 集成与 Webhook 网关 | PRD 8.1；`docs/prd/flows.md#41-github-事件驱动工作项流转与推送` | GitHub App 配置、签名校验、事件去重、队列消费、重试与死信、降级轮询 | 事件回放测试：重复投递不产生重复流转；3 秒内返回 200 |
| T-0602 | 关联与自动流转 | `docs/prd/flows.md#三事件驱动的自动流转` 全部 12 条规则 | 提交/分支/PR/流水线与工作项关联；AF-01 ~ AF-12 规则实现与开关 | 每条规则一个集成测试；守卫不满足时不流转并记录原因 |
| T-0603 | 部署与回滚 | PRD FR-DV-04 | 部署记录、回滚触发版本状态流转与复盘任务 | 回滚后版本状态与复盘任务正确创建 |
| T-0604 | 页面还原 | `devops.html` | `/devops`：仓库、分支、PR、流水线与日志、环境部署、代码质量 | 视觉回归 ≤ 1% |

**批次验收**：模拟 GitHub 事件可驱动看板卡片自动流转并推送通知；页面还原达标。

---

## M7 测试与质量

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0701 | 用例库与覆盖矩阵 | PRD FR-TS-01 | 用例 CRUD、状态机、需求-用例矩阵、未覆盖 AC 标红 | 覆盖率计算与手工一致 |
| T-0702 | 测试计划与执行 | PRD FR-TS-02 | 计划、执行轮次、结果回写工作项、全通过自动流转待验收 | 自动流转守卫（无未关闭 P0/P1）生效 |
| T-0703 | 缺陷生命周期 | PRD FR-TS-03；状态机文档缺陷章节 | 严重程度与 SLA（P0 15 分钟/4 小时 …）、重开计数、≥2 次标质量风险 | SLA 与升级测试；重开规则测试 |
| T-0704 | 质量门禁 | PRD FR-TS-04 | 六项门禁判定、阻断、超管豁免与审计、纳入版本复盘 | 回归通过率边界（97.9/98/98.1）；豁免留痕 |
| T-0705 | 漏测反查 | **PRD 5.5**；`docs/prd/flows.md#26-漏测反查线上问题反向评估测试有效性` | 六步流水线、六类判定与责任环节、补测用例生成、相似度 0.72、周报推送、复核回流 | 六类判定各一个用例；阈值边界用例；判定只到环节不到个人 |
| T-0706 | 页面还原 | `testing.html` | `/testing` 全部页签含漏测分析 | 视觉回归 ≤ 1% |

**批次验收**：生产缺陷可自动触发反查并生成补测用例与改进项；门禁阻断生效；页面还原达标。

---

## M8 产品与发布

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0801 | 产品与模块 | PRD FR-PD-01 | 产品、能力模块、北极星指标、可见范围 | 产品详情 7 页签数据齐备 |
| T-0802 | 路线图 | PRD FR-PD-02/03/04 | 五视角路线图、客户反馈关联、对外同步开关与变更提醒 | 切换视角数据不变；未标记对外的条目在门户完全不可见 |
| T-0803 | 版本发布 | 状态机文档版本章节 | 版本状态机、发布就绪度、门禁结果聚合 | 状态机测试；就绪度计算正确 |
| T-0804 | 页面还原 | `product.html`、`product-detail.html` | `/product`、`/product/:code` | 视觉回归 ≤ 1% |

---

## M9 智能工时与成本

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-0901 | 采集 | PRD 5.4；`worklog.html` 采集开关 | commit / PR 评审 / CI 重跑 / 日历四类来源采集与开关 | 开关关闭后不再采集；采集不含代码内容 |
| T-0902 | 推算与归集 | **PRD 5.4 全部参数** | 编码/评审/联调/会议四段推算、0.5 小时取整、按编号归集、未归集看板 | 参数逐一验算的单测（含 90 分钟、25 分钟、180 行、10 小时上限边界）；归集率统计正确 |
| T-0903 | 确认与调整 | PRD FR-WL-03 | 成员确认、±30% 调整与超幅填因、周五未确认提醒 | 调整幅度边界（29.9%/30%/30.1%）；提醒定时任务测试 |
| T-0904 | 成本核算 | PRD FR-WL-04 | 三维度成本分摊、未归集治理、个人明细权限 | 成本计算与手工一致；非本人非主管访问明细返回 404 |
| T-0905 | 页面还原 | `worklog.html` | `/worklog` 全部页签含算法说明与开关 | 视觉回归 ≤ 1% |

---

## M10 度量与人效

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-1001 | 指标预聚合 | `docs/prd/metrics.md#八指标计算与存储` | 每小时增量抽取、`metric_snapshot` 写入、口径版本记录、下钻明细 | 快照与明细一致；口径变更不回刷历史 |
| T-1002 | 指标实现 | `docs/prd/metrics.md` 第一 ~ 四章 | DORA 四项 + 交付效率 12 项 + 质量 11 项 + 工时成本 9 项 | 每个指标一个手工可验算的单测；DORA 等级判定正确 |
| T-1003 | 人效与贡献 | `docs/prd/metrics.md#五人效与贡献指标` | 六维画像、产品贡献率、单点依赖（≥80%）、人才象限 | 贡献率各产品线内合计 100%；单点依赖阈值边界用例；权限校验 |
| T-1004 | 页面还原 | `metrics.html`、`people.html` | `/metrics`、`/people` | 视觉回归 ≤ 1% |

---

## M11 复盘与知识库

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-1101 | 复盘主流程 | PRD FR-RT-01/02；`docs/prd/flows.md#28-复盘与改进闭环` | 四类复盘、三种模板、匿名反馈、投票（5 票）、迭代数据自动带出 | 匿名不可反查（管理员也不可见） |
| T-1102 | 行动项闭环 | PRD FR-RT-03 | SMART 校验、生成任务、临期推送、二次延期升级、上次闭环率展示 | SMART 五项缺一不可保存；闭环率计算正确 |
| T-1103 | 复盘页面还原 | `retro.html`、`retro-detail.html` | `/retro`、`/retro/:id` | 视觉回归 ≤ 1% |
| T-1104 | 知识库还原 | `wiki.html` | `/wiki`：空间、模板、与工作项互链 | 视觉回归 ≤ 1% |

---

## M12 消息与业务方门户

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-1201 | 企业微信集成 | PRD 8.2 | 应用消息、群机器人、身份绑定、卡片按钮回调 | 卡片按钮点击可完成认领并回写状态 |
| T-1202 | 推送规则引擎 | PRD 8.2 的 24 条规则表 | WHEN/WHO/渠道/时机 可配置；模板与变量；订阅与免打扰；P0 与合规穿透 | 24 条规则逐条测试触发条件；免打扰与穿透规则测试 |
| T-1203 | 推送效果 | PRD FR-NT-03 | 送达/已读/点击统计、低打开率提示 | 统计口径与 `metrics.md` 一致 |
| T-1204 | 消息中心还原 | `notify.html` | `/notify` 全部页签 | 视觉回归 ≤ 1% |
| T-1205 | 业务方门户 | PRD FR-PO-01/02；`portal.html` | 独立只读身份、业务语言进度、对外路线图、订阅与变更提醒、字段隔离 | 门户身份看不到工时/人员/内部评论（用例断言接口层就已过滤）；视觉回归 ≤ 1% |

---

## M13 系统设置与整体收尾

| 任务 | 目标 | 输入 | 产出 | 验收 |
| --- | --- | --- | --- | --- |
| T-1301 | 系统设置 | PRD FR-ST-01 ~ 04；`settings.html` | 集成中心、成员与权限、工作项类型与颗粒度、状态机可视化配置、自定义字段、自动化规则、安全与审计 | 新增工作项类型与状态不发版即生效；删除有存量的状态被阻止；审计日志齐全 |
| T-1302 | 登录与身份 | `login.html` | 账号密码 + 企业微信扫码 + SSO 预留 + MFA | 视觉回归 ≤ 1%；登录态与权限联动 |
| T-1303 | 全量还原与串场 | 全部 24 页；`docs/dev/UI-PARITY.md` | 全量视觉回归；PRD 第十一章的端到端串场用例 | 24 页全部 ≤ 1%；串场用例全绿 |
| T-1304 | 非功能验收 | PRD 第七章 | 性能压测（首屏 1.5s、列表 500ms、Webhook 3s、拖拽 100ms）、降级演练、备份恢复演练 | 各项达标并留下报告 `docs/dev/NFR-REPORT.md` |
| T-1305 | 交付文档 | — | 部署手册、环境变量清单、运维手册、三份角色上手指南 | 按手册在干净环境能一次拉起 |

---

## 全局验收（全部批次完成后）

对照 `docs/PRD.md#十一验收与测试策略` 的七个层级逐项执行，输出 `docs/dev/ACCEPTANCE-REPORT.md`：功能验收、流程验收、状态机验收、集成验收、性能验收、权限验收、数据验收。
