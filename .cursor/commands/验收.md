对指定批次执行验收（我会告诉你批次号，如「验收 M3」）。

步骤：

1. 从 `docs/dev/BUILD-PLAN.md` 取该批次的全部验收项。
2. 逐项实际执行验证命令（不许凭代码推断结论），记录真实输出。
3. 对该批次涉及的页面跑 `pnpm e2e:parity`，报告差异比例。
4. 对照 `AGENTS.md` 的 DoD 六条逐任务核对。
5. 输出一张表：验收项 / 命令 / 实际结果 / 通过与否；不通过的列出具体差距与修复计划。

发现不通过项时，先修复再重新验收，不要只报告问题。
