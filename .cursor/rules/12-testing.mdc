---
description: 测试规范：单测覆盖哪些、状态机与算法怎么测、E2E 与视觉回归怎么组织。
globs: **/*.test.ts,**/*.spec.ts,e2e/**
alwaysApply: false
---

# 测试规范

## 必须有测试的四类代码

1. **状态机**：每条合法转换各一个用例；每条非法转换各一个用例断言被拒且错误里含未满足项；守卫的边界条件（如"回归通过率 98%"取 97.9 / 98 / 98.1）
2. **算法**：用 PRD 里的公式与参数造用例，覆盖边界（聚类窗口正好 90 分钟、单日超过 10 小时被截断、调整幅度正好 30%、相似度正好 0.78）
3. **指标**：每个指标函数一个用例，用手工可验算的小数据集，断言到具体数值
4. **权限**：每个受限接口至少三个用例——有权限、无权限（断言 404）、跨数据范围（断言 404）

## E2E（Playwright）

按 PRD 的端到端串场组织，至少覆盖：

- 工单创建 → 分诊转需求 → 评分排期 → 拆解推送项目 → 看板认领 → 提 PR（模拟 Webhook）→ 自动流转 → 测试通过 → 发布 → 通知
- 需求 DoR 未达标被拦截，补齐后放行
- 看板四种范围切换与 WIP 超限告警
- 越权访问返回 404

## 视觉回归

独立配置 `playwright.parity.config.ts`，基线来自原型页面本身（不是提交进仓库的旧截图），每次跑都是"应用 vs 当前原型"的实时对比。阈值 `maxDiffPixelRatio: 0.01`。

## 测试数据

统一用 `packages/shared/src/fixtures`（从原型抽取的那一份），不要在测试里另造数据。需要变体时基于 fixture 做局部覆盖。

## 禁止

- 用 `.skip` / `.only` 提交
- 断言只写 `expect(res.status).toBe(200)` 而不校验返回内容
- 为了让测试通过而放宽业务规则
