---
description: 测试规范：后端 JUnit 5 + Testcontainers，前端 Vitest，端到端与视觉回归用 Playwright。
globs: backend/**/src/test/**,frontend/**/*.spec.ts,frontend/**/*.test.ts,e2e/**
alwaysApply: false
---

# 测试规范

## 必须有测试的四类代码

1. **状态机**：每条合法转换各一个用例；每条非法转换各一个用例断言被拒且异常里含未满足项；守卫边界（如"回归通过率 98%"取 97.9 / 98 / 98.1）
2. **算法**：用 PRD 的公式与参数造用例，覆盖边界（聚类窗口正好 90 分钟、单日超过 10 小时被截断、调整幅度正好 30%、相似度正好 0.72 与 0.78）
3. **指标**：每个指标一个用例，用手工可验算的小数据集，断言到具体数值
4. **权限**：每个受限接口至少三个用例——有权限、无权限（断言 404）、跨数据范围（断言 404）

## 后端（JUnit 5）

- 领域层（状态机、算法、指标）写**纯单测**，不起 Spring 容器，速度要快
- Mapper 与集成逻辑用 **Testcontainers** 起真实 MySQL 8 / Redis / RabbitMQ，**不要用 H2**（H2 与 MySQL 在 JSON、排序规则、日期精度上行为不同，会掩盖问题）
- Controller 层用 `@SpringBootTest` + MockMvc 覆盖鉴权、参数校验、错误码
- 断言用 AssertJ；测试数据来自 `contracts/fixtures`，需要变体时基于 fixture 局部覆盖
- 每个测试方法名用中文描述行为：`void 需求未通过DoR时不允许进入评审()`

## 前端（Vitest）

- 组件测试用 `@vue/test-utils`，重点覆盖：条件渲染分支（卡片徽标、空状态、阻塞态）、格式化函数、深链解析、localStorage 偏好读写
- 不测样式细节（那是视觉回归的职责）

## 端到端（Playwright）

按 PRD 第十一章的串场组织，至少覆盖：

- 工单创建 → 分诊转需求 → 评分排期 → 拆解推送项目 → 看板认领 → 模拟 GitHub Webhook → 自动流转 → 测试通过 → 发布 → 通知
- 需求 DoR 未达标被拦截，补齐后放行
- 看板四种范围切换与 WIP 超限告警
- 越权访问返回 404

## 视觉回归

独立配置 `e2e/playwright.parity.config.ts`：基线是**原型页面本身**（起一个静态服务指向仓库根目录），对比对象是应用路由。每次跑都是"应用 vs 当前原型"的实时对比，阈值 `maxDiffPixelRatio: 0.01`，视口 1600×1200 与 1366×768 两档。

跑之前必须 `make seed`，保证数据与原型一致；时间戳与自增 ID 用 mask 屏蔽。

## 禁止

- 用 `.skip` / `.only` / `@Disabled` 提交
- 断言只写状态码而不校验返回内容
- 为了让测试通过而放宽业务规则
- 用 H2 替代 MySQL 做集成测试
