---
description: 跨语言领域契约与代码生成规范。后端 Java、前端 TS 共用同一份状态机 / 算法参数 / 枚举 / 指标口径定义。
alwaysApply: true
---

# 领域契约与代码生成

后端是 Java、前端是 TypeScript，无法共享一个领域包。为了让"同一条业务规则只定义一次"，把**状态机、算法参数、枚举、指标口径**下沉为语言中立的 YAML，由生成器同时产出两边代码。

```
contracts/domain/*.yaml ──▶ backend/devflow-domain/.../generated/*.java
                       └──▶ frontend/src/generated/*.ts
```

## 铁律

1. **改规则只改 YAML**，然后 `make codegen`。禁止直接改 `generated/` 下的文件（都带 `DO NOT EDIT` 头）。
2. CI 校验：重新生成后 `git diff --exit-code` 必须干净。生成物要提交进仓库（方便代码评审看到规则变化）。
3. 每条规则带 `prd:` 字段指向 PRD 锚点，`make check-prd` 校验有效性。
4. 生成物只包含**数据与常量**（状态表、转换表、权重、阈值、枚举、指标编码），业务逻辑各自手写，但必须消费这份数据，**禁止在 Java 或 TS 里硬编码任何阈值**。

## 契约文件

| 文件 | 内容 | PRD 出处 |
| --- | --- | --- |
| `contracts/domain/enums.yaml` | 全部枚举字典 | `docs/prd/data-model.md#四枚举字典` |
| `contracts/domain/state-machines/*.yaml` | 8 类对象的状态、归类、转换（触发 / 守卫 / 动作）、超时、可操作角色 | `docs/prd/state-machines.md` |
| `contracts/domain/algorithms.yaml` | 优先级六因素权重与五种算法、工时九个参数、漏测两个阈值、容量公式、WIP 规则 | `docs/PRD.md` 5.2 ~ 5.6 |
| `contracts/domain/metrics.yaml` | 指标编码、定义、公式、数据来源、统计维度 | `docs/prd/metrics.md` |
| `contracts/fixtures/*.json` | 从原型抽取的种子数据（后端 seed 与前端测试共用） | 原型页面内联脚本 |
| `contracts/openapi.yaml` | 接口契约，由后端 springdoc 产出 | — |

## YAML 写法示例

```yaml
# contracts/domain/algorithms.yaml
worklog:
  prd: docs/PRD.md#54-关键规则工时自动推算
  params:
    commitClusterWindowMinutes: { value: 90,  desc: 相邻提交间隔小于该值视为同一编码时段 }
    minCommitMinutes:           { value: 25,  desc: 单次提交最小计时，避免碎片提交低估 }
    baselineLinesPerHour:       { value: 180, desc: 每小时基准当量，按语言与复杂度加权 }
    codingCoefficient:          { value: 0.8 }
    dailyCapHours:              { value: 10 }
    prReviewMinutesPerRound:    { value: 20 }
    ciRetryMinutes:             { value: 15 }
    roundToHours:               { value: 0.5 }
    memberAdjustRatio:          { value: 0.3, desc: 成员可调整幅度 ±30% }
```

```yaml
# contracts/domain/state-machines/requirement.yaml
entity: requirement
prd: docs/prd/state-machines.md#一需求-requirement
states:
  - { code: PENDING_REVIEW, name: 待评审, kind: initial, timeoutDays: 2, roles: [产品经理, 产品总监] }
  # …
transitions:
  - id: R2
    from: PENDING_REVIEW
    to: IN_REVIEW
    trigger: manual
    guards: [DOR_ALL_PASSED]
    actions: [CREATE_REVIEW, NOTIFY_REVIEWERS, START_REVIEW_SLA]
```

## 生成器

`tools/codegen/` 用 Node 实现，读 YAML 产出：

- Java：枚举类、`Transitions.java`（不可变转换表）、`AlgorithmParams.java`（常量）、`MetricCodes.java`
- TS：`enums.ts`、`state-machines.ts`、`algorithm-params.ts`、`metric-codes.ts`

生成器本身要有单测：给一份小型 YAML，断言产出的 Java 与 TS 内容符合预期。

## 接口契约

后端 Controller 用 springdoc 注解写全，构建时产出 `contracts/openapi.yaml` 并提交；前端用 `openapi-typescript` 生成 `frontend/src/generated/api.d.ts`。**前端禁止手写接口类型**。接口变更流程：改后端注解 → 重新产出 openapi → 前端重新生成 → 一起提交。
